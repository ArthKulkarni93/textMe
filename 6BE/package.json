{
  "name": "6be",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node server.js",
    "build": "npx prisma generate && npm run dev",
    "start": "node server.js",
    "migrate": "npx prisma migrate deploy",
    "prisma:generate": "npx prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Real-time WebSocket app with Express, Prisma, and PostgreSQL",
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "prisma": "^6.5.0",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
